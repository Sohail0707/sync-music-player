{
  "name": "sync-music-player",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Synchronized real-time music listening for 20-30 friends, powered by LiveKit Cloud + Netlify.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "netlify:dev": "netlify dev"
  },
  "dependencies": {
    "livekit-client": "^2.9.0",
    "livekit-server-sdk": "^2.9.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.21.0"
  }
}
